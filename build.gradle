group 'springca'
version '1.0-SNAPSHOT'

allprojects {
    buildscript {
        repositories {
            maven { url centralMirror }
        }
    }
    repositories {
        maven { url centralMirror }
    }
}

